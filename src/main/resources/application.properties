# Tipo de persistencia activo. Usa "memory" para desarrollo sin base de datos o "mysql" para JDBC.
persistence.type=memory

# Configuración JDBC usada cuando persistence.type=mysql.
db.url=jdbc:mysql://localhost:3306/squarestruct?useSSL=false&serverTimezone=UTC
db.user=root
db.password=root
